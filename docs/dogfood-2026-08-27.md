# Clean-machine dogfood journal — 2026-08-27

This is the live journal for the post-pull Ullage reset and QA pass on the
macOS host. It records host-local actions and evidence; it is not a claim that
every Windows game has a usable renderer on every Wine/GPTK combination.

## Scope and baseline

* Repository: `main` at `c2915af` (`docs: record final lifecycle controls`),
  clean before the run.
* Steam root: `/Users/kurt/Library/Application Support/Steam`.
* Installed Steam depots at reset: 17 AppIDs, including the
  `Steamworks Common Redistributables` tool; 16 were game depots.
* The old Ullage state was about 3.0 GB. It contained a 2.2 GB Wine runtime,
  626 MB of duplicate appinfo backups, stale mappings, logs, experiments, and
  runtime/download state.

## Reset performed

1. Stopped the old `ullage-gui` process before touching its state.
2. Created the recoverable archive
   `/Users/kurt/Developer/ullage-preclean-20260827.tar.gz` (12,706,854 bytes).
   It preserves configuration, logs, evidence, cloud/launcher metadata,
   runtime manifests/provenance, and the pre-clean Steam appinfo plus depot
   mode file. The generated Wine tree and duplicate appinfo backup set were
   intentionally omitted to avoid retaining 2.8 GB of reproducible data.
3. Restored the one active foreign Katamari appinfo entry to its original
   `katamari.exe` launch entry before deleting Ullage state.
4. Cleared the old Windows depot override and verified Steam's
   `steam_dev.cfg` was empty.
5. Removed the exact `/Users/kurt/.ullage` directory, then verified the
   machine reported `needs_setup` rather than inheriting the previous state.
6. Verified Git remained clean and recovered about 1 GiB of APFS headroom
   immediately after the purge.

## Fresh runtime installation

`bin/ullagectl runtime fetch --json` installed the official locked runtime
artifact from `xXJSONDeruloXx/ullage-patches`:

* release tag: `runtime-macos-2026-08-26-3`
* asset: `ullage-bridge-runtime-2026.08.26-3.tar.gz`
* asset SHA-256:
  `9413215f4a72d19adf2fe2024d16b4e7755542b34db94ea694005ea580212a4b`
* package manifest SHA-256:
  `6d0fc5002c894f411f066bca172af6770bde71b258a25aa7e6bb8dba8e2567e6`
* source commit: `d2a5b75ed247f410cda746ca2cfe205e26f4f1ec`
* patch manifest SHA-256:
  `339d8b77a43af3ebc15cd91bc09d93159634658be10956108405bb396adc3df3`

`doctor --json` and `runtime verify --json` both returned `ok: true`; all
four packaged bridge artifacts matched their recorded size and hash. The
fresh state is currently about 162 MB after mappings and per-install safety
backups, rather than restoring the old multi-gigabyte runtime copy.

## Fresh-machine launch/stop pass

All mappings below use the GameHub `gamehub-container-5` prefix and the newly
installed runtime package. A `P` here means that the mapped Windows launch
boundary reached a live process and the native Steam Stop path produced a
clean receipt. It does not by itself claim a captured game renderer surface.

| AppID | Title | Arch | Launch | Stop receipt | Prefix | Notes |
| ---: | --- | :---: | :---: | :---: | :---: | --- |
| 3480 | Peggle Deluxe | win32 | P | P | P | Native Play/Stop control pair; Steam displayed the expected 32-bit warning. |
| 848350 | Katamari Damacy REROLL | win64 | P | P | P | Native Play/Stop control pair; `diagnose` reported healthy mapping, bridge, Cloud, and no residual processes. |
| 3590 | Plants vs. Zombies: GOTY | win32 | P | P | P | Steam Cloud Out of Date warning accepted with the user-approved Play anyway action. |
| 394160 | ARCADE GAME SERIES: PAC-MAN | win64 | P | P | P | Reached the mapped PAC-MAN executable and stopped cleanly. |
| 8400 | Geometry Wars: Retro Evolved | win32 | P | P | P | Reached the mapped Windows executable and stopped cleanly. |
| 442070 | Drawful 2 | win64 | P | P | P | Default standalone Windows entry reached a live process; unavailable `launch_mp.bat` remains disabled. |
| 449040 | Jesus Christ RPG Trilogy | win32 | P | P | P | Reached the trilogy process tree; native Stop reaped the session cleanly. |
| 567090 | 8-Bit Bayonetta | win32 | P | P | P | Reached the mapped Windows process and stopped cleanly. |
| 584400 | Sonic Mania | win32 | P | P | P | Reached the mapped Windows process and stopped cleanly. |
| 1018130 | Castle Break | win64 | P | P | P | Reached the mapped Windows process and stopped cleanly. |
| 1144770 | SLUDGE LIFE | win64 | P | P | P | Reached the mapped Windows process; a Steam Helper self-update invalidated one stale UI action, then the fresh Stop action succeeded. |

Every completed receipt above records the fresh runtime manifest hash,
`native_stop_observed: true`, `prefix_clean: true`, and `wine_exit: 143`.
The authoritative per-AppID evidence remains under `~/.ullage/logs/` and
`~/.ullage/sessions/` on the test host.

## Issues and decisions

* The default smoke fixture references TIS-100 (`370360`), which is not
  installed on this host. Running the smoke command with installed controls
  `3480` and `848350` is green; no depot was downloaded merely to satisfy a
  historical fixture.
* Steam's native 32-bit warning is informational on this macOS host. It did
  not prevent the mapped 32-bit titles from reaching their Windows processes.
* Steam Cloud warnings are allowed for this run by explicit user direction.
  No in-game save or account action is being performed; Cloud-native mapping
  remains a separate, deliberate test gate.
* The native Steam Helper target is the recorded UI target. The first restart
  did not bootstrap through the accessibility bridge, so the official native
  Steam bundle was reopened from the shell and the Helper target was
  reattached. Subsequent Play/Stop interactions were performed in Steam's
  normal library UI.

## Host-runtime audit and release boundary

The first post-QA audit checked the machine and the exact GameHub sources
before adding another large runtime to the clean state:

* `/Applications/Wine Staging.app` is a complete x86_64 Wine 11.10 build with
  MoltenVK, but no GPTK/D3DMetal and no native Steamworks acceptance evidence.
  Ullage now exposes it as an incomplete selectable provider instead of
  silently treating ordinary Wine as the tested GameHub stack.
* `/Users/kurt/Developer/gamenative-proton-wine-reference` is a shallow,
  73 MB `GameNative/proton-wine` `proton_10.0` source checkout at
  `147d88ee0c4eee8b356110dd0c53cc44b7515f73`; it has no built macOS runtime
  and no `lsteamclient` source/output. Its x86_64 build script is Android/
  Termux-specific, while `tools/gitlab/build-mac` is only a generic Wine CI
  recipe. It is prior art, not a drop-in provider.
* GameHub's Wine source archive is 438,382,677 bytes with SHA-256
  `e72b9016c1732955dc5b24a93e8c11e5611b9454e8e7cf1a04a68f1dd3f06fd0`.
  GameHub's GPTK 3.0-3 archive is 16,171,863 bytes with SHA-256
  `c02366780d7078c7b872c7eecff1157fe8579838a9ee3422295cecd5bc180c7b`.
* The disposable `runtime host-fetch` rehearsal verified both archives,
  preserved the required symlink layout, installed a clean Wine prefix, and
  passed `runtime host-verify` without GameHub.app or a GameHub process.
* The public release contains the exact clean Wine archive and the lock for
  the original GPTK source. It intentionally excludes the proprietary
  GameHub SandboxFS library, native Steam, and all account-bearing prefixes;
  the Apple-signed D3DMetal component remains an original-source/user-supplied
  prerequisite.

The release is published at
[`runtime-macos-gamehub-2026-08-27-1`](https://github.com/xXJSONDeruloXx/ullage-patches/releases/tag/runtime-macos-gamehub-2026-08-27-1).
Its Wine asset is `wine-proton_11.0_0728.tar.zst`, 438,382,677 bytes, with
SHA-256 `e72b9016c1732955dc5b24a93e8c11e5611b9454e8e7cf1a04a68f1dd3f06fd0`;
the attached lock is 2,336 bytes with SHA-256
`c3741da20f564b955009782e0091d19d26875b50643939a90ba03295f43d9e8f`.
The full documented command, with no local archive overrides, was run in a
disposable no-GameHub home and passed host installation plus layout
verification. That rehearsal was then removed to recover its 2.3 GB expanded
copy. The separate 881 MB source/archive audit staging directory was also
removed after the release hash check; it was re-downloadable evidence rather
than retained runtime state, and APFS headroom returned from 16 GiB to 17 GiB.

The source-built successor is now pinned for implementation work: Valve Wine
`proton_11.0` at `dc26e61847081a1b5cb0733dc30feba6ee575482`, plus the matching
`ullage-patches` series at `d2a5b75ed247f410cda746ca2cfe205e26f4f1ec`. The first
successor milestone is a complete x86_64 Wine build with lsteamclient, a clean
prefix, and an explicit renderer profile; a full Proton distribution build is
not required. A build was deliberately not started in this pass: Docker is
installed but its daemon is not running, Homebrew Bash 5 is not installed, and
the current free-space margin is too small for an unbounded source checkout and
build. No source-built runtime is represented as ready until those prerequisites
are provisioned and the control pair passes.

## METAL GEAR SOLID 4 boot investigation — AppID 2492670

The user downloaded the Windows depot for `METAL GEAR SOLID 4: Guns of the
Patriots - Master Collection Version`. Steam finished the staged update and
verification before the first test; the installed manifest reported build
`24921893`, depot `2492671`, and `24,501,734,674` bytes on disk. Ullage used
the fresh runtime package above, `gamehub-container-5`, the existing native
Steam client, and the default mapped entry `Launcher/launcher.exe`.

The investigation was run in progressively more specific layers:

1. Native Steam Play reached the mapped launcher and entered `App Running`.
   The launcher stayed alive without creating `mgs4.exe` or a usable game
   surface. A fresh capture showed the launcher-owned `Steam Error` dialog:
   `Application load error 3:0000065432`.
2. A direct diagnostic launch of the shipped launcher reproduced the same
   error. Wine relay tracing showed the launcher attempting to open
   `Local\\SteamStart_SharedMemLock` and
   `Local\\SteamStart_SharedMemFile`; both opens returned null before the
   launcher displayed the dialog. The trace also showed the launcher later
   calling `TerminateProcess(-1, 0x33)` on its failure path.
3. A separately staged, unmodified copy of the game's `steam_api64.dll`
   passed `SteamAPI_Init`, pipe/user creation, logged-on identity, matching
   AppID `2492670`, subscription, DLC enumeration, and shutdown through the
   same Ullage runtime. This rules out a basic lsteamclient load, identity, or
   ownership failure, but it is a diagnostic process rather than a claim that
   the shipped game reached its own API calls.
4. A read-only `tools/ullage-steamstart-probe.c` was compiled with MinGW and
   run while GameHub's SteamAgent launch was active. It returned
   `ERROR_FILE_NOT_FOUND` for both named objects. The GameHub log confirmed
   that its SteamAgent launched `launcher.exe`, then only tracked the
   launcher and crash handler; no `mgs4.exe` was created.
5. The local stock Valve Steam client DLL set was copied into a disposable
   prefix with the open-source reference launcher. `steamclient64.dll`,
   `tier0_s64.dll`, `vstdlib_s64.dll`, and the crash handler loaded, proving
   that the remaining gap is not simply a missing companion DLL. That prefix
   had no authenticated Steam session, and its Steam service could not start;
   the direct launcher still exited without a game surface.

Static inspection of the stock Valve `steamclient64.dll` showed that the
Windows-side Steam DRM service owns the 1 KiB shared mapping, initializes the
version/state fields, and consumes the launcher-written AppID/status fields.
The small relay experiment could acknowledge the launcher's event wait, but
that was not enough: the launcher then followed its DRM failure path. A
GameHub `SteamAgent.vmp.exe` launch is therefore not equivalent to a full
Windows Steam client/DRM service for this title.

The current result is deliberately recorded as a boundary, not a forced
“fix”: Ullage launch mapping, SteamAPI initialization/ownership, supervision,
and cleanup passed; MGS4 renderer/boot acceptance failed before `mgs4.exe`.
The native Stop path and direct diagnostic stop both left a clean prefix with
no residual Wine or game processes. No shipped executable was patched or
replaced. No Steamless swap, third-party DRM binary, or `OnlineFix`
`StubDRM64.dll` was used; the installed depot and account-bearing Steam state
remain the source of truth.

The durable diagnostic source is
[`tools/ullage-steamstart-probe.c`](../tools/ullage-steamstart-probe.c). The
next legitimate boot gate is a compatible Windows-side Steam DRM startup
service or a documented, redistributable title-compatible implementation;
adding a named-object acknowledgement alone is insufficient. This title is
not counted as a renderer pass until `mgs4.exe` reaches a visible surface.

## Native Play-anyway and SteamStart service boundary

After the first cloud-gated launch, the user-approved `Play anyway` action was
repeated against native Steam. Steam recorded the response as `IgnoreCloud`,
continued into `CreatingProcess`, and started the Ullage launcher at
`17:54:08`. This separates the cloud warning from the MGS4 failure: the
launcher still showed `Application load error 3:0000065432`, no `mgs4.exe`
appeared, and the run was stopped with a clean prefix.

For a bounded protocol experiment, Ullage ran the unmodified diagnostic
`tools/ullage-steamstart-server.c` in the same virtual prefix, with no account
or license data. The server created the two legacy named objects and logged
the actual request from the launcher:

```text
steamstart_request pid=196 event=<redacted> appid=2492670 ac=3073580850 status=51 b4=692768
steamstart_duplicate=1 signal=1 error=0
steamstart_request_complete state=0
```

The server therefore demonstrated the OS-level event/handle transfer that the
launcher was waiting for, but the launcher still returned the same DRM error
and never spawned `mgs4.exe`. The server is intentionally diagnostic-only: it
has no authenticated Steam backend, ownership-ticket service, or licensing
logic, and is not enabled by the normal Ullage launcher. This is evidence that
“create the mapping and acknowledge the event” is not a sufficient substitute
for Steam.

## ARM/Android precedent audit — 2026-08-27

The following local checkouts were fetched and advanced only when their work
trees were clean:

| Project | Revision examined | What it contributes |
| --- | --- | --- |
| `/Users/kurt/Developer/gamenative-reference` | `1ad70ae` | Current Android runtime/download packaging, FEX 2607/2608, Box64 0.4.4, WOWBox64, and the bionic Steam path. |
| `/Users/kurt/Developer/winnative-reference` | `2bcd0a3` (`v0.5.0-beta`) | A Windows-side launcher around the stock Valve `steamclient64.dll`. |
| `/Users/kurt/Developer/gamenative-proton-wine-reference` | `147d88e` | Current local Proton/Wine reference; no macOS build or lsteamclient artifact. |
| `/Users/kurt/Developer/winlator` | branch `3e2496f`; fetched `origin/main` at `fb66541` | Box64 package lineage and the Android X11/Wine substrate; the user branch was not merged. |
| `/Users/kurt/Developer/steam-android-runtime-research` | branch `6b9edc4`, dirty | Device-specific FEX 2607 + Proton 11 and Runtime 4 experiments; fetched only, no dirty work was merged. |

The refreshed sources produce three separate lessons:

1. GameNative's newest bionic-Steam path launches a dedicated host process,
   loads a real `libsteamclient.so`, supplies `Steam3Master` and
   `SteamClientService`, accepts the user's existing refresh-token session,
   waits for a ready state, and pre-warms app/PICS/ownership-ticket work. Its
   small TCP `SteamPipeServer` is only lifecycle plumbing; its request handler
   does not implement Steam tickets or title DRM.
2. WinNative's `wn-steam-launcher` is the closest design reference for a
   legitimate equivalent: load the user's/installed stock
   `steamclient64.dll`, acquire `IClientEngine`, create a global user, attach
   an authenticated session when available, and call the client's
   `IClientAppManager::LaunchApp`. It is a useful contract to study, not a
   license bypass or a drop-in macOS binary.
3. FEX and Box64 solve the ARM64-host to x86/x64-guest execution boundary.
   Proton, prefix preparation, Steam Runtime libraries, and Turnip/software
   graphics solve the next runtime boundary. None of those layers supplies
   the Windows-side SteamStart/DRM backend that MGS4 rejected in the native
   test.

No refreshed Android research file contains an MGS4-specific result, so no
Android success is being counted as evidence for this title. Apple Silicon
already has an x86_64 execution path through the current Wine/GPTK runtime;
the observed failure is earlier than rendering. The next high-value Ullage
work is therefore to model the authenticated Steam bootstrap/session boundary
and title-compatible SteamStart service, while preserving the shipped game
files and `steam_api64.dll` contract.

Compatibility experiments remain allowed only when they preserve the user's
Steam installation and are reversible—for example, changing a documented
runtime, prefix, or legitimate Steam-client component. Executable patching,
Steamless/OnlineFix-style license bypasses, and substituting a fake
`StubDRM64.dll` are explicitly not part of the legitimate pass. If a future
isolated experiment only demonstrates that the renderer works after DRM is
removed, it must be labeled as a lower-tier diagnostic and cannot advance the
MGS4 acceptance row.

## Checkpoint validation — 2026-08-27

The repository build target was already current (`make all` had nothing to
rebuild). The new diagnostic source compiled from the checkout with the
MinGW-w64 cross-compiler as a PE32+ x86-64 Windows executable. All independent
CLI, runtime, host-runtime, mapping, path, reaper, Cloud, bridge, and
Steamworks-probe tests passed.

The aggregate `make check` run stops at the existing `test_cli.py` depot-mode
fixture because native Steam is intentionally still running for the live
investigation; that fixture expects the production guard to reject mutation
while Steam is running. No test was made to bypass that guard, and native
Steam was not stopped merely to manufacture a green aggregate result. The
failure is environmental and precedes the remaining tests, which were run
individually and passed.

## Next gates

Continue the installed-library matrix serially, update this file after each
meaningful batch, and push checkpoints. Exercise the two-option Arena and
Megabonk launchers, verify Cloud-native behavior only with a protected save
boundary, rerun the complete checks, and investigate any reproducible Ullage
boundary failure before advancing coverage.
