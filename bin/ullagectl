#!/bin/sh
# Stable machine-facing entry point. The Python facade owns the JSON contract.
set -eu

SCRIPT_DIR=$(CDPATH= cd "$(dirname "$0")" && pwd)
PYTHON3_VALUE=${PYTHON3:-python3}
exec "$PYTHON3_VALUE" "$SCRIPT_DIR/ullage" "$@"
